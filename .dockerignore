node_modules
.next
out
build
coverage

.git
.gitignore

.env
.env.*
!.env.example

*.md
.DS_Store
npm-debug.log*

.vscode
.idea

# uploaded ticket photos never belong in the image — production reads/writes them through a
# Coolify Persistent Volume mounted over public/uploads at runtime, not at build time.
public/uploads
